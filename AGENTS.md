# AGENTS.md

See [`CLAUDE.md`](CLAUDE.md) for the guide for AI coding agents working in this repo
(commands, module layout, public API, conventions, and known gotchas such as the
DDA-only MS2/MGF writers and tdfpy API breaks). That file is canonical; this one exists
because some tools look for `AGENTS.md` rather than `CLAUDE.md`.
